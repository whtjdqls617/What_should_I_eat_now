import React from "react";
import { Text } from "react-native";

const ShowFoodList = () => {

	//state 토글 on off 하는 변수
	/*
	if 토글 on
		return ( 푸드리스트 보여주는 컴포넌트 렌더링 <- 좋아하는 음식 리스트 넣어줌)
	else
		return ( "" <- 싫어하는 음식 리스트 넣어줌)
	*/

	return (
		<>
		<Text>ShowFoodList</Text>
		</>
	);
};

export default ShowFoodList;